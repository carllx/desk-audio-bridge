# Windows Platform Space

This directory is designated for Windows-specific configurations, scripts, and components.

## Boundaries
- Modifiable by the Windows IDE agent session.
- Keep platform-specific logic and scripts isolated here.
- Do not commit machine-specific absolute paths, hardware UUIDs, or sensitive credentials.
