# macOS Platform Space

This directory is designated for macOS-specific configurations, scripts, and components.

## Boundaries
- Modifiable by the macOS IDE agent session.
- Keep platform-specific logic and scripts isolated here.
- Do not commit machine-specific absolute paths, hardware UUIDs, or sensitive credentials.
